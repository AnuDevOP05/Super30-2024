pipeline {
    agent any

    stages {
        stage('Git Checkout') {
            steps {
                echo 'checkout git repo'
            }
        }
        stage('build') {
            steps {
                echo 'Build stage'
            }
        }
        stage('merge') {
            steps {
                echo 'merging stage'
            }
        }
    }
}
